# Go
bin/
dist/
dist-native/
coverage.out
coverage.html
*.test
*.prof
.tools/

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp

# Local env
.env
.env.local

# Build artifacts
/pleno-dlp

# opf_native engine (ADR-0005): fetched C sources and the built static libs
# are reproducible via `make opf-native-deps`/`opf-native-lib` and never
# committed — the pure-Go default build must stay free of C sources.
/build/opf-native/
/pkg/piiengine/opfnative/cdeps/

# Claude scratch
.claude/worktrees/
.claude/_workspace/
.claude/scheduled_tasks.lock
/_workspace/
/_workspace_prev/

# Codex / agent harness mirrors of .claude/ + CLAUDE.md (locally regenerated;
# do not commit — they duplicate the tracked .claude/ tree and CLAUDE.md)
.agents/
.codex/
/AGENTS.md

# dlp-bench (bench/, issue #298): generated fixtures, cloned corpora, and
# results are all reproducible via `make bench` and never committed —
# the fixtures are live format-valid fake credentials (would trip push
# protection on sight; see bench/README.md), the clones are pinned
# upstream repos, and the results are a point-in-time measurement.
/bench/fixtures/synthetic/generated/
/bench/fixtures/synthetic/labels.json
/bench/.cache/
/bench/results/*.json
/bench/results/*.md
__pycache__/
*.pyc
