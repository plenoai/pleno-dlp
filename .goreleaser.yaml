version: 2
project_name: pleno-dlp

before:
  hooks:
    - go mod tidy

builds:
  - id: pleno-dlp
    main: ./cmd/pleno-dlp
    binary: pleno-dlp
    env:
      - CGO_ENABLED=0
    goos: [linux, darwin, windows]
    goarch: [amd64, arm64]
    flags:
      - -trimpath
    ldflags:
      - -s -w
      - -X main.version={{.Version}}
      - -X main.commit={{.Commit}}

archives:
  - name_template: "{{.ProjectName}}_{{.Os}}_{{.Arch}}"
    formats: [tar.gz]
    format_overrides:
      - goos: windows
        formats: [zip]
    files:
      - LICENSE
      - README.md

checksum:
  name_template: 'checksums.txt'

# Software Bill of Materials per release artifact. anchore/syft is the
# de-facto standard SBOM generator GitHub's attestation action accepts.
sboms:
  - artifacts: archive
    documents:
      - "{{ .ArtifactName }}.sbom.json"

release:
  prerelease: auto
  draft: false
  footer: |
    ---
    Verify the download against the signed checksums:
    ```
    sha256sum -c checksums.txt
    ```
    Build-provenance attestations (`gh attestation verify`) are published
    once this repository is public / GHAS-enabled; until then the release
    job skips attestation, so use the checksums above.

changelog:
  use: github
  sort: asc
  groups:
    - title: 'New detectors'
      regexp: '^.*?feat\(detectors\)'
      order: 0
    - title: 'New sources'
      regexp: '^.*?feat\(sources\)'
      order: 1
    - title: 'Engine and CLI'
      regexp: '^.*?feat'
      order: 2
    - title: 'Bug fixes'
      regexp: '^.*?fix'
      order: 3
    - title: 'Other'
      order: 999
